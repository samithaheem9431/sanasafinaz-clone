<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Sidebar Layout</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">


    <style>
        :root {
            --primary-color: #4a5568;
            --secondary-color: #00bfff; /* Explicitly defined sky blue */
            --background-light: #f7fafc;
            --text-dark: #2d3748;
            --text-muted: #718096;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif !important;
            background-color: var(--background-light) !important;
            margin: 0 !important;
            padding: 0 !important;
            display: flex !important;
            flex-direction: column !important;
        }


        .admin-sidebar {
            width: 280px;
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            background-color: white;
            box-shadow: 4px 0 10px rgba(0, 0, 0, 0.05);
            overflow-y: auto;
            z-index: 1; /* Ensure sidebar stays below the header */
        }

        .sidebar-profile {
            padding: 25px;
            text-align: center;
            border-bottom: 1px solid #edf2f7;
            background-color: var(--secondary-color); /* Sky color */
            color: white;
        }

        .profile-image {
            width: 100px;
            height: 100px;
            border-radius: 15px;
            object-fit: cover;
            margin-bottom: 10px;
            border: 3px solid white;
        }

        .sidebar-menu {
            padding: 15px;
        }

        .sidebar-nav-item {
            text-decoration: none;
            color: var(--text-dark);
            padding: 10px 15px;
            display: flex;
            align-items: center;
            border-radius: 5px;
            transition: background-color 0.3s ease, color 0.3s ease;
            font-weight: bold;
        }

        .sidebar-nav-item:hover,
        .sidebar-nav-item.active {
            background-color: var(--secondary-color);
            color: white;
        }

        .accordion-button {
            font-weight: bold;
            background: none;
            border: none;
            padding: 10px 0;
            color: var(--text-dark);
            transition: color 0.3s ease, background-color 0.3s ease;
        }

        .accordion-button:hover,
        .accordion-button:not(.collapsed) {
            background-color: var(--secondary-color);
            color: white;
        }

        .accordion-body {
            padding-left: 20px;
        }

        .sidebar-dropdown-item {
            padding: 8px 40px;
            color: var(--text-muted);
            text-decoration: none;
            display: block;
            transition: background-color 0.3s ease, color 0.3s ease;
        }

        .sidebar-dropdown-item:hover {
            background-color: var(--secondary-color);
            color: white;
        }

        .sidebar-menu p {
            color: var(--secondary-color);
            font-weight: bold;
        }

        .main-content {
            margin-left: 280px;
            padding: 20px;
            flex-grow: 1;
            overflow-y: auto;
            padding-top: 80px; /* Ensure content starts below the header strip */
        }

        .header-strip {
            background-color: var(--secondary-color); /* Sky color */
            color: white;
            text-align: center;
            padding: 20px 0;
            font-size: 24px;
            font-weight: bold;
            position: fixed;
            top: 0;
            left: 280px; /* Move the strip below the sidebar */
            width: calc(100% - 280px); /* Cover the remaining body area */
            z-index: 2; /* Ensure header is above the sidebar */
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Add a small shadow for separation */
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .header-strip .logo {
            position: absolute;
            left: 20px;
            width: 40px; /* Set logo size */
            height: 40px;
            background-color: white; /* White color logo */
            border-radius: 50%; /* Circle */
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--secondary-color);
            font-size: 24px; /* Adjust icon size */
        }

    </style>
</head>
<body>

<!-- Header Strip (Fixed at the top, after the sidebar) -->
<div class="header-strip">
    <!-- User Logo (White color circle) -->
    <div class="logo">
        <i class="fas fa-user"></i> <!-- User icon -->
    </div>

    SANASAFINAZ ADMIN PANEL
</div>

<div class="admin-sidebar">
    <!-- Profile Section -->
    <div class="sidebar-profile">
        <img src="/images/d.png" alt="Admin Profile" class="profile-image">
        <h5 class="fw-bold">Eman Fatima</h5>
        <p class="text-light">Admin Manager</p>
    </div>

    <!-- Sidebar Menu -->
    <div class="sidebar-menu">
        <!-- Main Section -->
        <p class="text-uppercase small">Main</p>
        <a href="/admin/dashboard" class="sidebar-nav-item active">
            <i class="fas fa-home me-3"></i> Dashboard
        </a>
        <a href="/admin/analytics" class="sidebar-nav-item">
            <i class="fas fa-chart-line me-3"></i> Analytics
        </a>

        <a href="/" class="sidebar-nav-item">
            <i class="fas fa-home me-3"></i> Website
        </a>

        <!-- Product Management -->
        <p class="text-uppercase small mt-4">Product Management</p>

        <div class="accordion" id="productAccordion">
            <div class="accordion-item">
                <h2 class="accordion-header">
                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#productsDropdown" aria-expanded="false">
                        <i class="fas fa-box me-3"></i> Products
                    </button>
                </h2>
                <div id="productsDropdown" class="accordion-collapse collapse" data-bs-parent="#productAccordion">
                    <div class="accordion-body">
                        <a href="/admin/products/create" class="sidebar-dropdown-item">Add Product</a>
                        <a href="/admin/products" class="sidebar-dropdown-item">View Products</a>
                    </div>
                </div>
            </div>

            <div class="accordion-item">
                <h2 class="accordion-header">
                    <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#categoriesDropdown" aria-expanded="false">
                        <i class="fas fa-tags me-3"></i> Categories
                    </button>
                </h2>
                <div id="categoriesDropdown" class="accordion-collapse collapse" data-bs-parent="#productAccordion">
                    <div class="accordion-body">
                        <a href="/admin/products/createCategory" class="sidebar-dropdown-item">Add Category</a>
                        <a href="/admin/viewcatagories" class="sidebar-dropdown-item">View Categories</a>
                    </div>
                </div>
            </div>
        </div>

        <!-- User Management -->
        <p class="text-uppercase small mt-4">User Management</p>
        <a href="#" class="sidebar-nav-item">
            <i class="fas fa-users me-3"></i> User List
        </a>
        <a href="#" class="sidebar-nav-item">
            <i class="fas fa-user-plus me-3"></i> Add User
        </a>
    </div>

    <!-- Footer -->
    <div class="p-3 border-top">
        <a href="/" class="btn btn-danger w-100">
            <i class="fas fa-sign-out-alt me-2"></i> Logout
        </a>
    </div>
    
</div>

<div class="main-content">
    <%-body  %>
</div>

<!-- Optional JavaScript -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
